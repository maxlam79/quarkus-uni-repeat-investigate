# quarkus-uni-repeat-investigate
This is to investigate on the Uni/repeat/until collection result which doesn't include the last run iteration.

## To Run
```
./mvnw clean test
```

This will execute the `org.acme.UniRepeatTest`. This test will fail. 